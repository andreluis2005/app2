# Base Rackathon App (Coinbase Base)

Projeto simples React (Vite) criado para um rackathon da Base da Coinbase.
Pronto para enviar ao GitHub e fazer deploy no Vercel.

## Como usar
1. Clone ou descompacte o projeto.
2. No diretório do projeto, instale dependências:
   ```bash
   npm install
   ```
3. Rodar em desenvolvimento:
   ```bash
   npm run dev
   ```
4. Build para produção:
   ```bash
   npm run build
   npm run preview
   ```
5. Para deploy no Vercel, conecte o repositório GitHub e deixe as configurações padrão do Vite.

## O que contém
- App React minimal com componentes organizados em `src/components`.
- Mock de "Conectar carteira" (sem integração real) para você estender com libs como wagmi/ethers.
- Arquivo `vercel.json` recomendando build settings.

Boa sorte no rackathon! 🚀
